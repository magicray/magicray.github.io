name: MagicRank

on:
  schedule:
    - cron: '0 0,15 * * *'
    
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build:
   runs-on: ubuntu-latest
   steps:
      - uses: actions/checkout@v2
      
      - name: magicrank
        run: |
          python3 -mpip install magicrank
          python3 -m magicrank --screen 467990/Profit-Growth-Value --hold 0.3 --top 500 --count 500 > magicrank.txt
          git config user.name github-actions
          git config user.email github-actions@github.com
          git commit -m 'Auto generated by Action' magicrank.txt magicrank.json
          git push
